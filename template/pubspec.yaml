name: example
description: A sample modular-api application.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  # modular_api: ^0.0.7 # In production, uncomment this line and remove the path dependency
  modular_api:
    path: ../../modular_api
  postgres: ^3.0.0
  
dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  http: ^1.5.0
